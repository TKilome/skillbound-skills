{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "diagnose_job",
  "type": "object",
  "required": ["jobManagerUrl"],
  "properties": {
    "jobManagerUrl": {"type": "string", "minLength": 1},
    "jobId": {"type": "string"},
    "httpHostHeader": {"type": "string", "description": "Optional HTTP Host header for NodePort or ingress access without DNS."},
    "report": {"type": "boolean", "description": "Return a structured health report with findings, root cause candidates, and recommendations."},
    "dryRun": {"type": "boolean"}
  },
  "additionalProperties": false
}
