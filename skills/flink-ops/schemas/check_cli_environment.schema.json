{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "check_cli_environment",
  "type": "object",
  "required": ["flinkHome"],
  "properties": {
    "deploymentTarget": {"type": "string", "enum": ["kubernetes", "yarn"]},
    "flinkHome": {"type": "string", "minLength": 1},
    "dryRun": {"type": "boolean"}
  },
  "additionalProperties": false
}
